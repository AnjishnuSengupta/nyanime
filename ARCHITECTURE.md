# 🏗️ NyAnime Complete Architecture Diagram

## High-Level Overview

```
┌──────────────────────────────────────────────────────────────────────────┐
│                           USER BROWSER                                   │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                      React Application                             │ │
│  │                     (localhost:8080 / Vercel)                      │ │
│  │                                                                    │ │
│  │  Components:                                                       │ │
│  │  ├─ Header (Search, Auth)                                         │ │
│  │  ├─ AnimeList (Browse)                                            │ │
│  │  ├─ AnimeDetails (Info + Episodes)                                │ │
│  │  └─ VideoPage                                                     │ │
│  │      └─ AnimePlayer                                               │ │
│  │          └─ VideoPlayer (iframe: embed-player.com)                │ │
│  │                                                                    │ │
│  │  Services:                                                         │ │
│  │  ├─ aniwatchApiService.ts (API calls with CORS handling)          │ │
│  │  ├─ firebaseAuthService.ts (User authentication)                  │ │
│  │  └─ animeDataService.ts (Data management)                         │ │
│  └────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────┘
           │                          │                          │
           │ API Calls                │ Video Streaming          │ Auth
           ▼                          ▼                          ▼
┌──────────────────────┐   ┌────────────────────┐   ┌──────────────────┐
│   CORS Proxy Layer   │   │  embed-player.com  │   │ Firebase Auth    │
│   (Auto-Rotation)    │   │   (HLS Player)     │   │  + Firestore     │
│                      │   │                    │   │                  │
│ 1. allorigins.win    │   │ Features:          │   │ Features:        │
│ 2. corsproxy.io      │   │ • HLS playback     │   │ • Google login   │
│ 3. codetabs.com      │   │ • CORS handling    │   │ • Watch history  │
│ 4. Custom (optional) │   │ • Quality select   │   │ • Favorites      │
└──────────┬───────────┘   └────────────────────┘   └──────────────────┘
           │
           ▼
┌───────────────────────────────────────────────────────────────────────┐
│                        Aniwatch API Backend                           │
│                   (aniwatch-latest.onrender.com)                      │
│                                                                       │
│  Endpoints:                                                           │
│  ├─ /api/v2/hianime/search?q={query}                                │
│  ├─ /api/v2/hianime/anime/{id}/episodes                             │
│  └─ /api/v2/hianime/episode/sources?animeEpisodeId={id}&server={s}  │
│                                                                       │
│  Returns:                                                             │
│  • Anime metadata                                                     │
│  • Episode lists                                                      │
│  • M3U8 streaming URLs                                               │
│  • Subtitles                                                          │
└─────────────────────────────┬─────────────────────────────────────────┘
                              │
                              ▼ (Scrapes)
                    ┌──────────────────────┐
                    │     hianime.to       │
                    │  (Source Website)    │
                    └──────────────────────┘
```

---

## Detailed Request Flow

### 1. Search for Anime

```
┌─────────┐
│  User   │ Types "Steins;Gate" in search
└────┬────┘
     │
     ▼
┌──────────────────────┐
│  SearchBar.tsx       │
└────┬─────────────────┘
     │
     ▼
┌──────────────────────────────────────────┐
│  aniwatchApiService.searchAnime()        │
│                                          │
│  const url = CORS_PROXY +               │
│    encodeURIComponent(                   │
│      ANIWATCH_API_BASE_URL +            │
│      '/api/v2/hianime/search?q=' +      │
│      encodeURIComponent('Steins;Gate')  │
│    )                                     │
└────┬─────────────────────────────────────┘
     │
     ▼
┌────────────────────────────────────────────┐
│  https://api.allorigins.win/raw?url=       │
│  https://aniwatch-latest.onrender.com/     │
│  api/v2/hianime/search?q=Steins%3BGate    │
└────┬───────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────┐
│  Response:                          │
│  {                                  │
│    success: true,                   │
│    data: {                          │
│      animes: [                      │
│        {                            │
│          id: "steinsgate-3",        │
│          name: "Steins;Gate",       │
│          poster: "...",             │
│          episodes: { sub: 24 }      │
│        }                            │
│      ]                              │
│    }                                │
│  }                                  │
└─────────────────────────────────────┘
```

### 2. Get Episode List

```
┌─────────┐
│  User   │ Clicks on "Steins;Gate"
└────┬────┘
     │
     ▼
┌──────────────────────┐
│  AnimeDetails.tsx    │
└────┬─────────────────┘
     │
     ▼
┌──────────────────────────────────────────┐
│  aniwatchApiService.getEpisodes()        │
│                                          │
│  endpoint: /api/v2/hianime/anime/       │
│            steinsgate-3/episodes         │
└────┬─────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────┐
│  Response:                          │
│  {                                  │
│    success: true,                   │
│    data: {                          │
│      totalEpisodes: 24,             │
│      episodes: [                    │
│        {                            │
│          number: 1,                 │
│          title: "Turning Point",    │
│          episodeId: "steinsgate-   │
│                     3?ep=230"       │
│        },                           │
│        ...                          │
│      ]                              │
│    }                                │
│  }                                  │
└─────────────────────────────────────┘
```

### 3. Get Streaming Sources

```
┌─────────┐
│  User   │ Clicks "Episode 1"
└────┬────┘
     │
     ▼
┌──────────────────────┐
│  AnimePlayer.tsx     │
└────┬─────────────────┘
     │
     ▼
┌────────────────────────────────────────────────┐
│  aniwatchApiService.getStreamingSources()      │
│                                                │
│  endpoint: /api/v2/hianime/episode/sources?   │
│    animeEpisodeId=steinsgate-3?ep=230&        │
│    server=hd-1&                                │
│    category=sub                                │
└────┬───────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────┐
│  Response:                                  │
│  {                                          │
│    success: true,                           │
│    data: {                                  │
│      headers: {                             │
│        Referer: "https://hianime.to/"       │
│      },                                     │
│      sources: [                             │
│        {                                    │
│          url: "https://cdn.example.com/     │
│                video/master.m3u8",          │
│          quality: "1080p",                  │
│          isM3U8: true                       │
│        }                                    │
│      ],                                     │
│      subtitles: [...]                       │
│    }                                        │
│  }                                          │
└─────────────────────────────────────────────┘
```

### 4. Video Playback

```
┌──────────────────────┐
│  VideoPlayer.tsx     │
│                      │
│  Receives:           │
│  • M3U8 URL          │
│  • Headers           │
│  • Quality options   │
└────┬─────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  Build embed-player.com URL:                 │
│                                              │
│  https://embed-player.com/video/?            │
│    source=https%3A%2F%2Fcdn.example.com%2F  │
│           video%2Fmaster.m3u8&              │
│    autoplay=1&                               │
│    color=%23a855f7                          │
└────┬─────────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  <iframe                                     │
│    src="https://embed-player.com/video/..."  │
│    allow="autoplay; fullscreen"              │
│  />                                          │
└────┬─────────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│  embed-player.com internally:                │
│  1. Fetches M3U8 playlist                    │
│  2. Handles CORS with its backend            │
│  3. Loads video segments                     │
│  4. Displays player controls                 │
│  5. User watches video! 🎬                   │
└──────────────────────────────────────────────┘
```

---

## CORS Proxy Rotation Flow

```
┌──────────────────────────┐
│  API Request Started     │
│  (e.g., search anime)    │
└────────┬─────────────────┘
         │
         ▼
    ┌─────────────────────────────┐
    │ Try Proxy 1: allorigins.win │
    └────┬───────────┬────────────┘
         │           │
    Success?     Fail? (timeout/error)
         │           │
         ▼           ▼
    ┌────────┐  ┌──────────────────────┐
    │ RETURN │  │ Rotate to Proxy 2    │
    └────────┘  └────┬─────────────────┘
                     │
                     ▼
            ┌─────────────────────────┐
            │ Try Proxy 2: corsproxy  │
            └────┬───────────┬────────┘
                 │           │
            Success?     Fail?
                 │           │
                 ▼           ▼
            ┌────────┐  ┌──────────────────────┐
            │ RETURN │  │ Rotate to Proxy 3    │
            └────────┘  └────┬─────────────────┘
                             │
                             ▼
                    ┌─────────────────────────┐
                    │ Try Proxy 3: codetabs   │
                    └────┬───────────┬────────┘
                         │           │
                    Success?     Fail?
                         │           │
                         ▼           ▼
                    ┌────────┐  ┌──────────┐
                    │ RETURN │  │  ERROR   │
                    └────────┘  └──────────┘
```

---

## Serverless Function Flow (Optional)

When using custom streaming proxy:

```
┌──────────────────┐
│  VideoPlayer     │
└────┬─────────────┘
     │
     │ Build proxy URL:
     │ /api/stream?url=<m3u8_url>&h=<base64_headers>
     │
     ▼
┌────────────────────────────────────────┐
│  Vercel Function: /api/stream.ts       │
│                                        │
│  1. Parse query params                 │
│  2. Decode headers (base64 → JSON)     │
│  3. Fetch upstream M3U8                │
│  4. Check if playlist or segment       │
│  5. If playlist: rewrite URLs          │
│  6. Add CORS headers                   │
│  7. Return response                    │
└────┬───────────────────────────────────┘
     │
     ▼
┌────────────────────────────────────────┐
│  Upstream CDN                          │
│  (https://cdn.example.com/)            │
│                                        │
│  Returns: M3U8 playlist or video chunk │
└────┬───────────────────────────────────┘
     │
     ▼
┌────────────────────────────────────────┐
│  Proxy rewrites M3U8:                  │
│                                        │
│  Before:                               │
│  segment001.ts                         │
│  segment002.ts                         │
│                                        │
│  After:                                │
│  /api/stream?url=https://cdn...001.ts  │
│  /api/stream?url=https://cdn...002.ts  │
└────┬───────────────────────────────────┘
     │
     ▼
┌────────────────────────────────────────┐
│  Browser receives proxied playlist    │
│  All requests go through /api/stream   │
│  CORS headers present ✅               │
└────────────────────────────────────────┘
```

---

## File Structure

```
nyanime/
├── src/
│   ├── components/
│   │   ├── AnimePlayer.tsx       ← Loads streaming data
│   │   ├── VideoPlayer.tsx       ← Renders video player
│   │   └── ...
│   ├── services/
│   │   ├── aniwatchApiService.ts ← CORS proxy + API calls
│   │   ├── firebaseAuthService.ts
│   │   └── ...
│   ├── config/
│   │   └── apiConfig.ts          ← API configuration
│   └── ...
├── api/
│   └── stream.ts                 ← Vercel streaming proxy
├── functions/
│   └── stream.ts                 ← Cloudflare streaming proxy
├── .env                          ← Environment variables
├── .env.example                  ← Template
├── STREAMING_GUIDE.md            ← Full documentation
├── QUICK_FIX.md                  ← Deployment guide
└── SUMMARY.md                    ← This summary
```

---

## Data Flow Summary

```
User Action → React Component → Service Layer → CORS Proxy → API Backend
                                                                  ↓
                                                            Returns Data
                                                                  ↓
                                            Process → Update State → Render
                                                                  ↓
                                            Video Player (iframe) → Play
```

---

## Technology Stack Visual

```
┌─────────────────────────────────────────────────────────────┐
│                    Frontend (Browser)                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  React 18 + TypeScript                              │   │
│  │  ├─ Vite (Build Tool)                               │   │
│  │  ├─ Tailwind CSS (Styling)                          │   │
│  │  ├─ Radix UI (Components)                           │   │
│  │  ├─ React Router (Navigation)                       │   │
│  │  └─ Tanstack Query (Data Fetching)                  │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    APIs & Services                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Aniwatch API (Render.com)                          │   │
│  │  ├─ Node.js Backend                                 │   │
│  │  ├─ Web Scraping (hianime.to)                       │   │
│  │  └─ REST API Endpoints                              │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Firebase (Google)                                  │   │
│  │  ├─ Authentication (Email + Google)                 │   │
│  │  ├─ Firestore (Database)                            │   │
│  │  └─ Real-time Sync                                  │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  CORS & Streaming                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  CORS Proxies                                       │   │
│  │  ├─ allorigins.win                                  │   │
│  │  ├─ corsproxy.io                                    │   │
│  │  └─ codetabs.com                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Video Player                                       │   │
│  │  ├─ embed-player.com (HLS)                          │   │
│  │  └─ Optional: Custom Proxy Functions                │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Deployment                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Vercel (Primary)                                   │   │
│  │  ├─ Static Hosting                                  │   │
│  │  ├─ Serverless Functions                            │   │
│  │  └─ Edge Network (CDN)                              │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Cloudflare Pages (Alternative)                     │   │
│  │  ├─ Pages Functions                                 │   │
│  │  └─ Global CDN                                      │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

**Created:** November 23, 2024  
**Author:** NyAnime Development Team  
**Status:** ✅ Complete
